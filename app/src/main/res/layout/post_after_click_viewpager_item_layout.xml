<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">



    <include
        android:id="@+id/typeWriterLayout"
        layout="@layout/typewriter_progressbar_snippet"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:layout_centerInParent="true"
        />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/main_iV_afterClick"
        android:scaleType="matrix"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="35dp"
        android:layout_gravity="center"
        android:background="@drawable/four_side_rounded_button_articles_seemore"
        android:text="Loading Failed! Retry"
        android:textColor="@color/white"
        android:textSize="11sp"
        android:id="@+id/retryBtn"
        android:visibility="gone"
        android:layout_below="@id/typeWriterLayout"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:layout_marginStart="90dp"
        android:layout_marginEnd="90dp"
        android:textAllCaps="false"
        />

</RelativeLayout>