package com.homie.psychq.main.ui.feeds;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

public class FeedsFragmentTest {

    @Before
    public void setUp() throws Exception {
    }


    @Test
    public void testNavigationToInGameScreen() {

        // Create a mock NavController
//        NavController mockNavController = mock(NavController.class);
//
//        // Create a graphical FragmentScenario for the TitleScreen
//        FragmentScenario<FeedsFragment> titleScenario = FragmentScenario.launchInContainer(FeedsFragment.class);
//
//        // Set the NavController property on the fragment
//        titleScenario.onFragment(fragment ->
//                Navigation.setViewNavController(fragment.requireView(), mockNavController)
//        );

        // Verify that performing a click prompts the correct Navigation action
//        onView(ViewMatchers.withId(R.id.play_btn)).perform(ViewActions.click());
//        verify(mockNavController).navigate(R.id.action_title_screen_to_in_game);
    }
    @After
    public void tearDown() throws Exception {
    }
}